function stringManipulation(str, start, count) {
    let end = start + count;

    console.log(str.substring(start, end));
}
stringManipulation('ASentence', 1, 8);
stringManipulation('SkipWord', 4, 7);